import 'package:flutter/material.dart';

class CostosTotalesPage extends StatelessWidget {
  const CostosTotalesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Costos Totales')),
      body: const Center(child: Text('Aquí va Costos Totales')),
    );
  }
}
