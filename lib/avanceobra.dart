import 'package:flutter/material.dart';

class AvanceObraPage extends StatelessWidget {
  const AvanceObraPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Avance de Obra')),
      body: const Center(child: Text('Aquí va Avance de Obra')),
    );
  }
}
