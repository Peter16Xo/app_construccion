import 'package:flutter/material.dart';

class PersonalAsignadoPage extends StatelessWidget {
  const PersonalAsignadoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Personal Asignado')),
      body: const Center(child: Text('Aquí va Personal Asignado')),
    );
  }
}
