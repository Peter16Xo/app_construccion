import 'package:flutter/material.dart';

class MaterialesUsadosPage extends StatelessWidget {
  const MaterialesUsadosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Materiales Usados')),
      body: const Center(child: Text('Aquí va Materiales Usados')),
    );
  }
}
